@model Inzynierka.Data.Tables.Cruises

@{
    ViewData["Title"] = "Utwórz Rejs";
}

<h1>Utwórz Rejs</h1>

<h4>Resjy</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="name" class="control-label"></label>
                <input asp-for="name" class="form-control" />
                <span asp-validation-for="name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="description" class="control-label"></label>
                <input asp-for="description" class="form-control" />
                <span asp-validation-for="description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="destination" class="control-label"></label>
                <input asp-for="destination" class="form-control" />
                <span asp-validation-for="destination" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="start_date" class="control-label"></label>
                <input asp-for="start_date" class="form-control" min="@DateTime.Now.ToString("yyyy-MM-dd")" />
                <span asp-validation-for="start_date" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="end_date" class="control-label"></label>
                <input asp-for="end_date" class="form-control" min="@DateTime.Now.ToString("yyyy-MM-dd")" />
                <span asp-validation-for="end_date" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="price" class="control-label"></label>
                <input asp-for="price" class="form-control" />
                <span asp-validation-for="price" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="currency" class="control-label"></label>
                <input asp-for="currency" class="form-control" />
                <span asp-validation-for="currency" class="text-danger"></span>
            </div>
        @*     <div class="form-group">
                <label asp-for="status" class="control-label"></label>
                <select asp-for="status" class="form-control"></select>
                <span asp-validation-for="status" class="text-danger"></span>
            </div> *@
            <div class="form-group">
                <label asp-for="maxParticipants" class="control-label"></label>
                <input asp-for="maxParticipants" class="form-control" />
                <span asp-validation-for="maxParticipants" class="text-danger"></span>
            </div>
          @*   <div class="form-group">
                <label asp-for="YachtId" class="control-label"></label>
                <select asp-for="YachtId" class ="form-control" asp-items="ViewBag.YachtId"></select>
            </div>
            <div class="form-group">
                <label asp-for="CapitanId" class="control-label"></label>
                <select asp-for="CapitanId" class ="form-control" asp-items="ViewBag.CapitanId"></select>
            </div> *@
            <div class="form-group">
                <input type="submit" value="Utwórz Rejs" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Pwrót</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

 @* <script>
    document.getElementById("submitBtn").addEventListener("click", function (e) {
        const startDateInput = document.getElementById("start_date");
        const endDateInput = document.getElementById("end_date");

        const startDate = new Date(startDateInput.value);
        const endDate = new Date(endDateInput.value);
        const today = new Date();
        today.setHours(0, 0, 0, 0); // Ustaw godziny na 00:00:00

        // Resetowanie poprzednich komunikatów o błędach
        startDateInput.classList.remove("is-invalid");
        endDateInput.classList.remove("is-invalid");

        let isValid = true;

        // Sprawdzenie, czy data rozpoczęcia jest w przyszłości lub dzisiaj
        if (startDate < today) {
            startDateInput.classList.add("is-invalid");
            alert("Data rozpoczęcia nie może być wcześniejsza niż dzisiaj!");
            isValid = false;
        }

        // Sprawdzenie, czy data zakończenia jest w przyszłości lub dzisiaj
        if (endDate < today) {
            endDateInput.classList.add("is-invalid");
            alert("Data zakończenia nie może być wcześniejsza niż dzisiaj!");
            isValid = false;
        }

        // Sprawdzenie, czy data rozpoczęcia jest wcześniejsza lub równa dacie zakończenia
        if (startDate > endDate) {
            startDateInput.classList.add("is-invalid");
            endDateInput.classList.add("is-invalid");
            alert("Data rozpoczęcia nie może być późniejsza niż data zakończenia!");
            isValid = false;
        }

        // Jeśli walidacja nie przeszła, zapobiegaj wysłaniu formularza
        if (!isValid) {
            e.preventDefault();
        }
    });
</script> *@